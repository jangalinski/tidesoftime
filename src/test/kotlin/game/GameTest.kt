package com.github.jangalinski.tidesoftime

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

internal class GameTest {

//  val orderedGame = Game(deck = Deck(shuffle = false))
//
//  @Test
//  internal fun `each player is dealt 5 cards`() {
//    orderedGame.deal(5)
//
//    assertThat(orderedGame.playerData1.hand)
//        .hasSize(5)
//        .containsExactly(Card.DER_PASS_DES_ALTEN_MANNES, Card.DAS_BAD_DER_GOETTER, Card.DIE_ZITADELLE_DER_PROPHETEN, Card.DAS_AUGE_DES_NORDENS, Card.DER_GOLDENE_TEMPELTURM)
//    assertThat(orderedGame.playerData1.kingdom).isEmpty()
//
//    assertThat(orderedGame.playerData2.hand)
//        .hasSize(5)
//        .containsExactly(Card.DAS_KOENIGSNEST, Card.DIE_BLUTTRAENENQUELLE,Card.DAS_DACH_DER_WELT,Card.DIE_ALTERTUEMLICHE_KLUFT,Card.DER_VORHOF)
//    assertThat(orderedGame.playerData2.kingdom).isEmpty()
//
//
//  }
//
//  @Test
//  internal fun `game starts with shuffled deck and empty players`() {
//    val game = Game()
//
//    assertThat(game.playerData1.hand).isEmpty()
//    assertThat(game.playerData1.kingdom).isEmpty()
//    assertThat(game.playerData2.hand).isEmpty()
//    assertThat(game.playerData2.kingdom).isEmpty()
//
//    assertThat(game.deck.remaining()).isEqualTo(18)
//  }
}